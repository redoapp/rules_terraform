load("//terraform:rules.bzl", "tf_project")

tf_project(
    name = "tf",
    data = ["data.txt"] + glob(["*.tf"]),
    providers = [
        "@terraform_provider_random//:provider",
        "//terraform/runfiles:provider",
    ],
)
